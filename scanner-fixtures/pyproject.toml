[project]
name = "scanner-fixtures"
version = "0.0.0"
description = "Fixture sources for LLM-call scanner validation (not intended to run)"
readme = false
requires-python = ">=3.11"
dependencies = [
  "openai>=1.0.0",
  "anthropic>=0.40.0",
  "langchain-openai>=0.2.0",
  "google-generativeai>=0.8.0",
  "llama-index-core>=0.11.0",
]

[tool.pytest.ini_options]
# Fixtures only — tests may not exist.
pythonpath = ["src"]
